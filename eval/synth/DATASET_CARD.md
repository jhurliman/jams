# ES2-synth-DnB — synthetic drum & bass stem corpus (dataset card)

**Purpose.** In-domain fine-tune training data for SCNet stem separation on drum & bass. No openly
licensed corpus of real, produced EDM multitracks exists (ES1 licensing research, verified from
three angles), so the corpus is synthetically rendered from ship-clean tools. Implements the
pre-registered ES2 spec (`paper/EXPERIMENTS.md`). Generated by `eval/synth/` (this repo).

**This card documents the generator + corpus definition.** The rendered audio is NOT committed
(regenerable, tens of GB); the code, manifests, splits and this card are the shipped artifacts.

## Target definition (musdb-XL style, binding)

- GT stems are the **premaster** buses: `drums`, `bass`, `other`, `vocals`. They sum to the
  **premaster mix** exactly (float residual ≈ −215 dB; a ~−90 dB 16-bit-FLAC quantization floor
  on disk). All per-stem processing (sidechain duck, saturation, convolution reverb + width, glue
  comp) is baked into each stem *before* the linear sum.
- The **master** chain (comp + brickwall limiter to a sub-style LUFS target, −8…−5 LUFS club) runs
  on the **mix only**. Each track records its premaster→master gain ratio so stems stay
  reconstructible. **Training against limited-master-summing stems is prohibited.**
- `vocals` is **true silence** (instrumental D&B pilot); each track's manifest marks it so the
  fine-tune can mask the vocals loss term. No synthetic near-silence noise is written.

## Sub-style coverage & corpus stats

- **n tracks: 500**; total audio: **10.3 GB** (FLAC 44.1 kHz/16-bit); total duration: 433 min;
  render wall-clock: **32 min** (8 workers, ~2–3.8 s/track).
- Per sub-style counts (balanced): jump-up 84, neurofunk 84, liquid 83, jungle 83, techstep 83,
  dancefloor 83.
- Sub-styles: jump-up, neurofunk, liquid, jungle/ragga, techstep, dancefloor — each with its
  characteristic drum pattern grammar, bass-design family and arrangement.
- Tempo 160.9–175.9 BPM (mean 172.6); all 12 key roots; modes natural/harmonic minor, dorian,
  phrygian; arrangements 34–40 bars (~30–56 s); master LUFS −8.8…−6.3 (club).

## Diversity axes (randomized per track; anti-one-template)

Tempo (160–176, sub-style triangular modes; jungle 160–174), key + mode (natural/harmonic
minor, dorian, phrygian), chord progression, two-drop arrangement grammar (phrase-quantized
intro→build→drop1→break→build2→drop2→outro), drum family + real-kit source + ghost density +
break-layer, bass family (sub + reese/wobble/jumpup-bounce/foghorn/growl, ≥2 layers), synth layer
count + roles, sidechain style (DnB duck vs housier pump) + depth, master loudness.

### Multi-engine timbral diversity (the #1 synth→real transfer lever)

Every melodic voice is a **procedurally randomized** patch (no factory-preset content) drawn
across **three distinct synthesis engines**, so the corpus is not monotimbral:

- **Surge XT** — per-track randomized osc engine (Classic subtractive, Wavetable, Window, Modern,
  FM2/FM3, Twist macro, String physical-model), filter model, waveshaper, unison, envelopes.
- **Dexed** — DX7-style **FM** (randomized algorithm feedback, operator ratios/levels/envelopes).
- **Vitalium** — **wavetable** (randomized wave-frame morph, spectral unison, filter, envelopes).

Drums combine our original DSP kit with **real one-shots from two kits** (per-hit pitch/gain
jitter + round-robin to kill the machine-gun tell), re-sequenced into D&B patterns:

- **E-GMD** (Roland TD-17, CC-BY 4.0) — isolated GM-keyed hits sliced via aligned MIDI.
- **TidalCycles TR-808** (CC0-1.0) — electronic one-shots.

**Realized distribution (from `diversity_report.json`, 500 tracks):**
- Synth-voice engine mix: **Surge 1547 / Vitalium-wt 727 / Dexed-FM 331** voices.
- Surge osc engines used: Sine 508, Classic 257, Modern 238, Wavetable 235, Twist 106, FM3 70,
  Window 65, FM2 59, String 9.
- Bass families: sub 500 (every track), reese 233, growl 204, wobble 130, jumpup-bounce 112,
  foghorn 36. Bass layers/track: 2 (297), 3 (191), 4 (12). Synth layers/track: 1–4.
- Drum real-source: E-GMD 293, TR-808 207. Patterns: twostep_classic 196, rolling 118,
  twostep_jumpup 56, amen_like 52, techstep_min 45, liquid_soft 33.
- Sidechain: duck 417, pump 83.

## Splits

Frozen, sub-style-balanced **ES2-synth-val** (~20%), disjoint from train, chosen deterministically
by seed. `split.json` records the exact `train`/`val` track-id lists and a `val_sha256`.
- val n: 102; train n: 398; **val_sha256: `2f637df802963cc100cbb385cadc1a6c8376497124862348422687172ff287da`**

## Validation

- **Stem-sum residual** (premaster stems → premaster mix): float ≈ **−218…−222 dB** (per-track,
  the true GT guarantee); on-disk FLAC-16 ≈ **−77…−80 dB** (16-bit quantization floor, effectively
  perfect for stem targets). See `validation_report.json`.
- **Shipped-SCNet SI-SDR realism** (single-pass SCNet XL IHF on rendered premaster mixes vs GT
  stems, 18 tracks spanning sub-styles): the per-stem SI-SDR **distribution** sits in **regime (a)**
  — an imperfect, real-music difficulty band (drums ≈ 5–17, bass ≈ 4–15, other ≈ −6–12 dB), not
  trivially clean and not broken; the spread (incl. genuinely hard tracks) is the signal we want.
  Full table in `validation_report.json`. (`other` lifted from the probe's 1.8 dB via the richer
  mid arrangement + convolution reverb.)

## Asset / license ledger

| Asset | License | URL | Use |
|---|---|---|---|
| E-GMD (Expanded Groove MIDI Dataset, Roland TD-17) | **CC-BY 4.0** | magenta.tensorflow.org/datasets/e-gmd | real drum one-shots (sliced via MIDI), re-sequenced |
| TidalCycles TR-808 (sounds-tr808-fischer) | **CC0-1.0** | github.com/tidalcycles/sounds-tr808-fischer | real CC0 808 one-shots, re-sequenced |
| Surge XT | GPL-3 + output grant | surge-synthesizer.github.io | bass + synth voices (procedural params) |
| Dexed | GPL-3 | asb2m10.github.io/dexed | FM voices (own patches) |
| Vitalium (DISTRHO/Vital, NO_AUTH) | GPL-3 | github.com/DISTRHO/DISTRHO-Ports | wavetable voices (content-free, own params) |
| DawDreamer | MIT | github.com/DBraun/DawDreamer | offline render engine |

Excluded on license grounds: any copyrighted "Amen" recording; Cymatics packs (EULA bars
redistributing isolated sounds — a separation stem *is* the sample); anything NonCommercial/unclear.

## Known limitations (honest)

- **Instrumental pilot**: `vocals` bus is silent by design; a vocal-D&B corpus is future work.
- **E-GMD source**: the staged manifest is one drummer's kit; per-hit pitch/gain jitter + the CC0
  808 kit + DSP kit provide additional drum-timbre variety, but real-break timbral breadth is
  bounded. No copyrighted breakbeats are used; jungle "chopped break" layers are re-sequenced from
  real CC0/CC-BY one-shots, not a sampled amen.
- **Condensed arrangements** (~30–56 s): section ORDER + phrase quantization follow the two-drop
  grammar, but per-section bar counts are compacted to bound corpus size to offline disk.
- Synth→real transfer is unproven until the fine-tune gate (a separate, pre-registered step).
